
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% FUNCTION: determines GCD of two positive integers (X,Y)
%
%           output: GCD
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

function gcd_val = determine_GCD(X,Y)


% first find divisors of both X and Y
x_Divs = determine_Divisors(X);

y_Divs = determine_Divisors(Y);